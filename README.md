# neural--network
Слайд 1
Разработка полносвязной нейронной сети на базе фреймворка TensorFlow Python для автоматического подбора косметических средств по типу кожи и визуальному анализу

Выполнили: Кашина Виктория и Федорова Екатерина

Слайд 2
Проблема и актуальность

Выбор косметики — сложная задача для потребителя из-за огромного ассортимента и индивидуальных особенностей кожи. Неправильно подобранное средство может привести к:

Неэффективности (потрачены деньги, нет результата).

Ухудшению состояния кожи (раздражение, аллергия, акне).

Потере времени и доверия к брендам.

Цифры рынка:

Глобальный рынок косметики оценивается в сотни миллиардов долларов.

Более 60% потребителей сталкиваются с трудностями при выборе средств.

Спрос на персонализированную косметику и цифровые консультации растет на 15-20% ежегодно.

Цель проекта: Создать интеллектуальную систему на основе машинного обучения, которая по описанию типа кожи и визуальным признакам (через анализ текста описания проблем) автоматически рекомендует подходящие косметические средства и категории ухода.

Слайд 3
Обзор архитектуры решения

В коде представлена полносвязная нейронная сеть (Fully Connected Neural Network) с последовательной архитектурой (Sequential), адаптированная для задачи многоклассовой классификации.

Тип: Feed-forward нейронная сеть (полносвязная).

Задача: Классификация средств по категориям (например: увлажняющий крем, очищающее средство, сыворотка с гиалуроновой кислотой и т.д.).

Активация: ReLU для скрытых слоев, Softmax для выходного.

Функция потерь: Categorical Crossentropy.

Выход: Вероятность принадлежности к каждой категории косметических средств.

Слайд 4
📊 АРХИТЕКТУРА МОДЕЛИ

Общая структура:
Входной слой (признаки типа кожи) → Скрытые слои (4) → Выходной слой (категории средств)

Подробная схема слоев:

Входной слой:

Форма: (n_features,) где n_features = количество признаков (тип кожи, проблемы, возраст и т.д.).

Скрытый слой 1:

Dense(256, activation='relu') - 256 нейронов.

BatchNormalization() - нормализация.

Dropout(0.3) - регуляризация.

Скрытый слой 2:

Dense(128, activation='relu') - 128 нейронов.

BatchNormalization()

Dropout(0.25)

Скрытый слой 3:

Dense(64, activation='relu') - 64 нейрона.

BatchNormalization()

Dropout(0.2)

Скрытый слой 4:

Dense(32, activation='relu') - 32 нейрона.

BatchNormalization()

Dropout(0.15)

Выходной слой:

Dense(num_categories, activation='softmax') - нейронов = количеству категорий средств.

Слайд 5
🎯 КЛЮЧЕВЫЕ ХАРАКТЕРИСТИКИ АРХИТЕКТУРЫ

Тип архитектуры: Последовательная, полносвязная.

Паттерн "Воронки": 256 → 128 → 64 → 32 → n_categories. Постепенное сжатие и абстрагирование признаков.

Комплексная регуляризация:

Адаптивный Dropout: Уменьшается с глубиной (0.3 → 0.15).

Batch Normalization: На каждом скрытом слое для стабилизации обучения.

Функции активации:

ReLU на скрытых слоях для нелинейности и скорости.

Softmax на выходе для получения распределения вероятностей по категориям.

Оптимизатор: Adam с learning_rate=0.0005.

Функция потерь: categorical_crossentropy (для многоклассовой классификации).

Метрики: Accuracy, Precision, Recall, AUC.

🔍 Особенности для задачи подбора косметики:

Модель учится находить сложные, неочевидные связи между комбинациями проблем кожи и ингредиентами/категориями средств.

Архитектура позволяет легко масштабировать количество выходных категорий (новые бренды, линии).

Регуляризация критически важна для избегания "переобучения" на ограниченных или несбалансированных данных о косметике.

Слайд 6
🏗️ АЛГОРИТМЫ, РЕАЛИЗОВАННЫЕ В СИСТЕМЕ

1. Алгоритм обработки текстовых описаний (NLP Preprocessor)

Тип: Алгоритм обработки естественного языка (NLP).

Как работает: Преобразует текстовые жалобы пользователя ("сухость, шелушение, чувствительность") в числовой вектор (Bag of Words / TF-IDF).

Вход: Свободный текст.

Выход: Векторизованные признаки для объединения с остальными данными.

2. Алгоритм балансировки категорий косметики (ClassBalancer)

Тип: Жадный алгоритм / SMOTE (Synthetic Minority Over-sampling Technique).

Проблема: В данных может быть много "очищающих средств" и мало "сывороток с ретинолом".

Решение: Балансирует датасет, искусственно увеличивая количество примеров редких категорий.

3. Алгоритм адаптивного обучения (AdaptiveScheduler)

Тип: Алгоритм планирования скорости обучения.

Как работает: ReduceLROnPlateau — автоматически снижает learning_rate, если качество модели на валидации перестает расти, позволяя "настроиться" на более точные веса.

4. Алгоритм "Умного" Dropout (SmartDropout)

Тип: Адаптивный алгоритм регуляризации.

Логика: Высокий dropout в первых слоях (борьба с шумом в описаниях), меньший — в последних (сохранение специфичных признаков для точной классификации).

5. Алгоритм ранней остановки (EarlyStoppingGuard)

Тип: Алгоритм контроля обучения.

Цель: Останавливает обучение, когда модель начинает переобучаться (растет ошибка на валидации), возвращая веса лучшей эпохи.

Слайд 7
🔄 ВЗАИМОДЕЙСТВИЕ АЛГОРИТМОВ В СИСТЕМЕ

Последовательность работы пайплайна:

Пользовательский ввод: Текст + структурированные данные (возраст, тип кожи).

NLP Preprocessor: Преобразует текст в вектор.

Data Merger: Объединяет все признаки в единый вектор.

ClassBalancer (на этапе подготовки данных): Балансирует датасет.

Нейронная сеть с SmartDropout: Выполняет классификацию.

AdaptiveScheduler & EarlyStoppingGuard: Контролируют процесс обучения, обеспечивая лучшее качество.

Выход: Топ-3 рекомендуемые категории средств с вероятностями.

🎯 Синергия: NLP-модуль извлекает смысл из текста, полносвязная сеть находит сложные зависимости, а алгоритмы контроля гарантируют, что модель надежно обучится и не переобучится на маркетинговые "шумные" описания.

Слайд 8
📊 ОПИСАНИЕ ГИПЕРПАРАМЕТРОВ И ДАННЫХ

⚙️ Гиперпараметры обучения:

learning_rate=0.0005 (Adam)

batch_size=32

epochs=100 (с ограничением ранней остановкой)

loss='categorical_crossentropy'

metrics=['accuracy', 'precision', 'recall']

📈 Параметры модели (пример для 50 признаков и 20 категорий):

Слой 1: (50 * 256) + 256 = 12,956

Слой 2: (256 * 128) + 128 = 32,896

Слой 3: (128 * 64) + 64 = 8,256

Слой 4: (64 * 32) + 32 = 2,080

Выходной: (32 * 20) + 20 = 660

Итого: ~56,848 обучаемых параметров.

📋 Пример датасета (структурированная часть):

age (число)

skin_type (категория: сухая, жирная, комбинированная, нормальная)

concern_1, concern_2 (категория: морщины, акне, пигментация, etc.)

sensitivity (бинарный: да/нет)

text_description (текст: "зимой кожа стянутая и краснеет")

target (категория средства: "Гидратирующий крем для сухой кожи", "Успокаивающая сыворотка", etc.)

Слайд 9
🎯 ВЫВОДЫ И РЕЗУЛЬТАТЫ

✅ Достигнутые результаты:

Создан прототип рабочей системы: Архитектура успешно классифицирует средства по типам кожи.

Решена задача многомодальности: Эффективно объединены структурированные данные и текстовые описания.

Обеспечена стабильность: Механизмы регуляризации (Dropout, BatchNorm) и контроля (EarlyStopping) предотвращают переобучение.

Интерпретируемость: На выходе — понятный список категорий с вероятностями, а не "черный ящик".

📈 Ожидаемые метрики качества:

Accuracy (Точность): > 85% на сбалансированном тестовом наборе.

Precision (Точность) для редких категорий: Критически важный параметр, чтобы не рекомендовать агрессивное средство чувствительной коже.

Recall (Полнота): Способность находить все возможные подходящие средства для запроса пользователя.

Слайд 10
🔮 ПЕРСПЕКТИВЫ РАЗВИТИЯ

1. Улучшение архитектуры:

Внедрение сиамских сетей для поиска "похожих" пользователей и их успешных выборов.

Использование предобученных эмбеддингов (BERT) для более глубокого анализа текста.

Добавление рекомендательных алгоритмов (Collaborative Filtering) на основе поведения пользователей.

2. Расширение функционала:

Интеграция с компьютерным зрением: Анализ фотографии кожи для объективной оценки состояния (пор, морщин, покраснений).

Учет ингредиентов: Модель сможет рекомендовать не только категорию, но и конкретные компоненты (гиалуроновая кислота, церамиды, SPF), а также исключать аллергены.

Персонализация по сезону и геолокации: Учет влажности, УФ-индекса, температуры.

3. Внедрение и бизнес-применение:

Чат-бот / Мобильное приложение: Пользовательский интерфейс для консультаций.

Платформа для брендов и ритейлеров: Инструмент анализа спроса и персонализированных предложений в онлайн-магазинах.

Система управления знаниями: Постоянное обновление модели на основе новых исследований, отзывов и продуктов.

🚀 Заключение: Разработанная нейросетевая модель закладывает основу для интеллектуальной косметической консультации. Она демонстрирует, как машинное обучение может решать практические задачи, повышая эффективность выбора средств, удовлетворенность клиентов и снижая риски от неправильного использования косметики.
